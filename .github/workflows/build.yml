name: Build

on: push

jobs:
  build:
    name: Build

    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v6
        with:
          submodules: 'true'

      - uses: actions/setup-java@v5
        with:
          java-version: 17
          distribution: zulu

      - uses: gradle/actions/setup-gradle@v6

      - name: Substitute Gradle settings file
        shell: bash
        run: ./substitute-settings.py
        env:
          COMPILER_VERSION: "2.0.0-SNAPSHOT.067"
          VALIDATION_VERSION: "2.0.0-SNAPSHOT.462"
          CORE_JVM_VERSION: "2.0.0-SNAPSHOT.523"
          CORE_JVM_COMPILER_VERSION: "2.0.0-SNAPSHOT.090"

      - name: Build project
        shell: bash
        # For the reason on `--no-daemon` see https://github.com/actions/cache/issues/454
        run: ./gradlew build --stacktrace --no-daemon
